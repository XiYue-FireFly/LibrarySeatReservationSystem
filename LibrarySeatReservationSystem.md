# 实验室座位预约系统

使用前后端的 vue3 和 java 开发，分管理端 + 学生端

## 学生端

登录（使用学号 + 密码登录，密码错误 5 次锁定账号 1 小时，登录后生成 token 保持登录状态）

注册（有定义用户名头像，输入姓名，使用学号 + 二次密码注册，学号唯一不可重复，密码需二次确认一致）

页面分为预约界面和个人中心界面

### 预约界面

#### 预约

通过前端选择实验室，进入实验室，通过点击选择前端界面上的座位进行预约

（需要自动捕获用户信息（学号 + 姓名）和选择预约时间，预约时间不可以超过 2 小时，不允许预约已过期时间，预约时间段不能与本人已有预约冲突）

前端返回进行二次提示是否确定，确认后提交预约。

#### 批量预约

用户一次性批量预约多个座位功能

（自动捕获用户的信息 + 选择预约时间，预约时间不可以超过 2 小时，同一用户单次不可预约超过 3 个座位，不允许预约同一时间段重复座位，所选座位必须为同一实验室）

前端返回进行二次提示是否确定预约，确认后提交。


***座位规则：座位绿色表示没有人，白色 + 头像有人，红色维护，在预约之后绿色变成白色加头像。座位状态实时刷新，过期预约自动释放座位。***

---

## 现已实现的文件与架构 (2026-03-13 更新)

### 1. 后端微服务架构 (Spring Cloud)
- **网关 (`library-gateway`)**: 负责 JWT 鉴权与路由分发。
- **用户服务 (`user-service`)**: 登录加锁（5次错误锁1小时）、头像 Redis 缓存。
- **预约服务 (`reservation-service`)**: 
  - `AdminSeatController.java`: 座位扩缩容、批量状态切换、行列自动命名、单座重命名。
  - `AdminLabController.java`: 实验室上下线（一键取消相关预约）。
  - `StudentBookController.java`: 预约冲突校验、取消预约逻辑。
  - `StudentLabController.java`: 座位状态查询（含实时头像透传）。
- **公共模块 (`library-common`)**: JWT 工具类、统一响应结果。

### 2. 前端界面 (Vue 3 + Vite)
- **首页 (`Home.vue`)**: 实验室大厅、实时座位图（支持头像占用显示）、状态图例修复。
- **管理后台 (`Dashboard.vue`)**: 
  - 预约管理（记录查询、带原因取消）。
  - 座位运维（动态列数、自定义每排布局、批量命名）。
  - 反馈处理（回复与处理状态同步）。
- **个人中心 (`Profile.vue`)**: 类型过滤 Tabs、新版取消确认弹窗、反馈自动关联坐标。

### 个人中心界面

1. 修改用户名信息功能（修改用户名，用户名不可包含特殊字符，修改后实时生效）
2. 查看预约功能（通过一张张卡片查看预约信息，卡片显示用户名字 + 学号 + 姓名 + 哪个实验室 + 哪个位置 + 预约时间段 + 预约状态）
3. 取消预约功能（在查看预约功能的界面支持单个取消或者批量取消预约，取消后座位自动恢复为空闲状态）
4. 反馈功能（在用户查看预约信息的卡片有反馈按钮，自动带入预约的位置和实验室，选择反馈类型并填写文字描述内容即可提交，提交后可查看反馈状态与管理员回复）

## 管理端

1. 登录（使用工号 + 密码登录，密码错误 5 次锁定账号 1 小时）
2. 注册（有定义用户名头像，输入姓名，使用工号 + 二次密码注册，工号唯一不可重复）
3. 管理员查看实验室座位预约信息的功能（可以通过点击对应座位看到该用户的信息（用户名字 + 学号 + 预约时间段 + 预约的座位信息），支持按实验室、时间、学号筛选）
4. 管理整个实验室是否可以预约功能（上线和下线（管理端下线之后用户进入用户端进行预约时告示不可以预约，请联系管理员），如果在管理员下线预约实验室时，有用户预约了，管理员可以一键取消所有已经预约用户的预约信息并且统一发出通知给预约了的用户，在用户登录时展示通知）
5. 取消用户预约的功能（在查看预约的界面的预约座位信息的后面有一个取消预约的功能，取消时可填写原因，取消后通知对应用户）
6. 修改实验室座位的个数和预约信息和座位维修状态（支持单个 / 批量设置座位为正常或维护状态，可调整实验室总座位数）
7. 反馈功能（接受用户的反馈，查看反馈详情，可对反馈进行回复，并修改反馈处理状态）

#### （一）通用基础功能（学生端 + 管理端）

| 功能 |                             规则                             |                           开发细节                           |
| :--: | :----------------------------------------------------------: | :----------------------------------------------------------: |
| 登录 | 1. 学生：学号 + 密码；管理员：工号 + 密码2. 密码错误累计 5 次，账号锁定 1 小时3. 支持 “记住密码”（本地加密存储，有效期 7 天）4. 登录后生成 JWT token（有效期 2 小时，过期自动登出） | 1. 后端对密码进行 MD5 + 盐值加密存储（不可逆）2. 前端存储 token 到 localStorage，请求头携带 token 验证3. 后端记录登录日志（时间、IP、设备） |
| 注册 | 【学生端】1. 必填项：学号（唯一）、姓名、密码（二次确认）、用户名、头像（默认 / 上传）2. 学号格式校验（如 8 位数字，可配置规则）3. 注册后自动激活（无需审核）【管理端】1. 必填项：工号（唯一）、姓名、密码（二次确认）、用户名、头像2. 仅超级管理员可创建普通管理员账号（禁止自主注册） | 1. 前端实时校验学号 / 工号唯一性（调用后端接口）2. 密码强度校验（至少 8 位，含数字 + 字母）3. 头像支持 jpg/png 格式，大小≤2MB |

#### （二）学生端核心功能（无验证码 + 反馈无图片）

##### 1. 预约界面（核心优化）

|  子功能  |                             规则                             |                       前端 / 后端约束                        |
| :------: | :----------------------------------------------------------: | :----------------------------------------------------------: |
| 单个预约 | 1. 选实验室→选座位→选预约时间（开始时间为整点 / 半点，如 14:00，结束时间 = 开始 + 1/2 小时，≤2 小时）2. 自动填充用户信息（学号 + 姓名，不可修改）3. 选座后锁定该座位 1 分钟（未确认则释放）4. 二次提示：显示 “你将预约 XX 实验室 XX 座位，时间 XX-XX，是否确认？” | 1. 前端：时间选择器限制结束时间≤开始 + 2 小时，禁用过去时间2. 后端：校验该座位同一时间段未被预约、用户无同时间段预约冲突 |
| 批量预约 | 1. 最多选择 3 个座位，超过 3 个自动禁用选中2. 支持 “连续座位筛选”（如选 1 号后自动推荐 2、3 号）3. 二次提示：显示 “你将批量预约 XX 实验室 XX、XX、XX 座位，时间 XX-XX，共 3 个，是否确认？”4. 批量预约的座位需属于同一实验室、同一时间段 | 1. 前端：选座数量≥4 时弹窗提示 “最多预约 3 个座位”，并禁用多选2. 后端：校验选中座位均未被预约，且数量≤3 |
| 座位状态 | 1. 绿色：空闲（可预约）2. 黄色：预约中（已预约未签到，开始前 15 分钟未签到自动释放）3. 白色 + 头像：已使用（签到后）4. 红色：维护（标注维护原因 + 预计恢复时间）状态实时刷新（每 30 秒） | 1. 前端：不同状态座位样式区分明显，红色座位 hover 显示维护原因2. 后端：定时任务清理过期预约（未签到 / 已结束），更新座位状态 |

##### 2. 个人中心界面（核心优化）

|   子功能   |                             规则                             |                           开发细节                           |
| :--------: | :----------------------------------------------------------: | :----------------------------------------------------------: |
| 修改用户名 | 1. 仅可修改用户名（学号 / 姓名不可改）2. 用户名长度 2-10 位，禁止特殊字符3. 修改后实时生效，头像同步可修改 |    前端：输入框校验字符规则，修改后调用接口并刷新个人信息    |
|  查看预约  | 1. 卡片展示：用户名 + 学号 + 姓名 + 实验室 + 座位号 + 预约时间段 + 状态（待签到 / 已签到 / 已结束 / 已取消）2. 支持按时间（近 7 天 / 近 30 天）、实验室筛选3. 卡片可滑动查看更多 |  前端：卡片布局适配移动端 / PC 端，筛选功能默认显示 “全部”   |
|  取消预约  | 1. 单个取消：卡片右上角 “取消” 按钮，需提前 1 小时取消（如 14:00 预约，13:00 前可取消）2. 批量取消：勾选多个预约卡片，一键取消3. 取消后弹窗提示 “取消成功”，座位状态恢复为绿色 | 1. 前端：超时不可取消的按钮置灰，提示 “需提前 1 小时取消”2. 后端：取消后更新预约状态，释放座位 |
|  反馈功能  | 1. 卡片内 “反馈” 按钮，必填项：实验室 + 座位号 + 反馈类型（遗失物品 / 座位故障 / 其他）+ 描述（≥10 字）2. 反馈后可查看状态（待处理 / 处理中 / 已解决），管理员回复后可查看 | 1. 前端：反馈类型下拉选择，描述输入框校验字数（≥10 字），无图片上传入口2. 后端：反馈记录关联预约 ID，支持按实验室筛选 |

#### （三）管理端核心功能（无验证码 + 反馈无图片）

|    子功能    |                             规则                             |                           开发细节                           |
| :----------: | :----------------------------------------------------------: | :----------------------------------------------------------: |
| 查看预约信息 | 1. 按实验室 / 时间段 / 学号筛选座位预约信息2. 点击座位显示详情：用户名 + 学号 / 工号 + 姓名 + 预约时间段 + 签到状态3. 支持导出预约记录（Excel 格式） | 前端：可视化座位图，点击座位弹窗展示详情，导出按钮支持选择时间段 |
| 实验室上下线 | 1. 下线：标记实验室为 “不可预约”，学生端进入该实验室提示 “该实验室暂不可预约，请联系管理员”2. 下线时如有已预约用户：一键取消所有预约，生成系统通知（内容：“XX 实验室因 XX 原因下线，你的预约已取消，敬请谅解”）3. 上线：恢复 “可预约” 状态，清除下线原因 | 1. 前端：下线时弹窗提示 “该实验室有 XX 个已预约用户，取消后将发送通知，是否确认？”2. 后端：取消预约后记录原因，通知关联用户 ID |
| 取消用户预约 | 1. 单个取消：在预约详情后点击 “取消”，需填写取消原因（下拉选择：违规预约 / 座位维修 / 其他）2. 取消后自动发送通知给用户 | 前端：取消原因必填，下拉选项固定，提交后提示 “取消成功，已通知用户” |
|   座位管理   | 1. 单个 / 批量修改座位数量（如 1 号实验室从 50 座改为 60 座）2. 标记座位状态：维护（填写原因 + 预计恢复时间）/ 空闲3. 支持可视化调整座位布局（拖拽） | 1. 前端：批量操作需二次确认，维护状态标注红色 + 原因2. 后端：座位信息修改后同步更新预约规则 |
|   反馈管理   | 1. 按实验室 / 反馈状态 / 时间筛选反馈2. 查看反馈详情（仅文字），可回复反馈（文字）3. 标记反馈状态（待处理 / 处理中 / 已解决） | 1. 前端：回复框仅支持文字输入，标记状态后同步推送给用户2. 后端：反馈回复记录关联用户 ID，用户登录可查看 |
|   通知管理   | 1. 查看所有系统通知（取消预约 / 实验室下线 / 反馈回复）2. 支持手动发送通知（按学号 / 实验室批量发送） |        后端：通知记录存储，用户登录时优先显示未读通知        |
|   数据统计   | 1. 核心指标：日 / 周 / 月预约量、座位使用率、取消率、反馈数量2. 图表展示（柱状图 / 折线图）3. 支持导出统计报表 |          前端：ECharts 可视化图表，支持切换时间维度          |



